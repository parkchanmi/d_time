<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="main">

<select id="notice_main" resultType="model.Notice_DTO">
select * from (select * from notice order by n_date desc) where rownum &lt; 3
</select>

<select id="board_main" resultType="model.Board_DTO">
select * from (select * from board order by b_date desc) where rownum &lt; 10
</select>

<select id="store_1" resultType="int">
select count(*) from store group by s_type having s_type='직영'
</select>
<select id="store_2" resultType="int">
select count(*) from store group by s_type having s_type='가맹'
</select>


</mapper>